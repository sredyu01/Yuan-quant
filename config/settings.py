# -*- coding: utf-8 -*-
"""
配置模块 - settings.py
存放全局配置信息，包括MT5账户、交易品种、时间框架等基础参数
"""

import MetaTrader5 as mt5

# ─────────────────────────────────────────────
# MT5 账户配置
# ─────────────────────────────────────────────
MT5_ACCOUNT = {
    "login":    277525304,                  # MT5 账号
    "password": "123456",                   # MT5 密码
    "server":   "Exness-MT5Trial5",         # 经纪商服务器名称
    "path":     "",                         # MT5 终端可执行文件路径（留空则自动检测）
    "timeout":  60000,                      # 连接超时时间（毫秒）
}

# ─────────────────────────────────────────────
# 交易品种配置
# ─────────────────────────────────────────────
DEFAULT_SYMBOL = "EURUSD"
SYMBOLS = [
    "EURUSD",
    "GBPUSD",
    "USDJPY",
    "XAUUSD",
    "BTCUSD",
]

# ─────────────────────────────────────────────
# 时间框架映射（字符串 -> MT5 常量）
# ─────────────────────────────────────────────
TIMEFRAMES = {
    "M1":  mt5.TIMEFRAME_M1,
    "M5":  mt5.TIMEFRAME_M5,
    "M15": mt5.TIMEFRAME_M15,
    "M30": mt5.TIMEFRAME_M30,
    "H1":  mt5.TIMEFRAME_H1,
    "H4":  mt5.TIMEFRAME_H4,
    "D1":  mt5.TIMEFRAME_D1,
    "W1":  mt5.TIMEFRAME_W1,
    "MN1": mt5.TIMEFRAME_MN1,
}
DEFAULT_TIMEFRAME = "H1"

# ─────────────────────────────────────────────
# 风险管理配置
# ─────────────────────────────────────────────
RISK = {
    "default_lot":       0.01,   # 默认交易手数
    "max_lot":           1.0,    # 单笔最大手数
    "max_open_positions": 5,     # 最大持仓数量
    "risk_per_trade":    0.01,   # 单笔风险比例（账户净值的 1%）
}

# ─────────────────────────────────────────────
# 数据获取配置
# ─────────────────────────────────────────────
DATA = {
    "default_bars": 500,         # 默认拉取 K 线条数
    "max_bars":     5000,        # 最大拉取 K 线条数
}

# ─────────────────────────────────────────────
# 回测配置
# ─────────────────────────────────────────────
BACKTEST = {
    "initial_capital": 10000.0,  # 初始资金（美元）
    "commission":      0.0001,   # 每手手续费（按比例）
    "slippage":        1,        # 滑点（点数）
}

# ─────────────────────────────────────────────
# 日志配置
# ─────────────────────────────────────────────
LOGGING = {
    "level":      "INFO",                   # 日志级别：DEBUG / INFO / WARNING / ERROR
    "log_dir":    "logs",                   # 日志文件目录
    "log_file":   "yuan_quant.log",         # 日志文件名
    "max_bytes":  10 * 1024 * 1024,         # 单个日志文件最大 10MB
    "backup_count": 5,                      # 最多保留 5 个历史日志文件
}
